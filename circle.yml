machine:
  xcode:
    version: 7.3
dependencies:
  override:
    - git submodule sync --recursive
    - git submodule update --init --recursive
test:
  override:
    - bin/test iOS
experimental:
  notify:
    branches:
      only:
        - master
